buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.6.3'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.8'
}
